// so far this does nothing... 

// do i use it to write to json?

const fs = require('fs');
const path = require('path');

function getNotes(notesArray) {
  const result = notesArray;
  return result;
}